# Program Brief

## Purpose

Static photography portfolio for Claire Thomas. The site is a multi-page HTML/CSS/JS portfolio with a shared client-side renderer in `site.js`, a shared stylesheet in `styles.css`, and one lightweight HTML entrypoint per section.

## Important Files And Boundaries

- `site.js`: shared navigation, gallery data, detail-page rendering, and lightbox behavior.
- `styles.css`: shared site layout and page-specific styling.
- `*.html`: individual entrypoints that set `body[data-page]` and load the shared assets.
- `prints.html`: print-shop landing page. The actual checkout/fulfillment URL is configured in `printShopConfig.shopUrl` in `site.js`.
- `Place/`, `The Natural World/`, `Protests/`, `Shapes & Shadows/`, `Commissioned Work/`: image assets. Treat as content, not app logic.
- `scripts/`: utility scripts for Cloudinary upload workflows.
- `scripts/gelato-products.mjs`: dry-run, template validation, stale-product audit, and resumable product creation for Gelato + Shopify.
- Missing remote products are recreated even when an obsolete Gelato ID remains in local state.
- Repair stalled unpublished drafts with `node scripts/gelato-products.mjs --execute --visible --repair-created --repair-batch-photos 1`. The repair deduplicates overlapping Gelato pages, bounds each API request at one minute, recovers known products by exact ID, polls new products by exact ID for up to three hours, retries after one hour without progress, and only clears repair-owned or aged queued products.
- `.gelato-product-manifest.json`, `.gelato-product-state.json`, `.gelato-stale-products.json`: ignored local artifacts generated by the Gelato batch workflow.
- `.vercel/`: deployment linkage/config created by Vercel CLI.

Keep content image directories and generated deployment metadata out of scope unless the task specifically requires them.

## Run And Verify

- Local static check: `python3 -m http.server 8080`
- Open the relevant page directly in a browser, or hit `http://localhost:8080/<page>.html`
- For navigation changes, verify the sidebar link state and destination page.
- For layout changes, verify both desktop and narrow/mobile widths.

## Current Operating Brief

- Prefer small, attributable edits in `site.js`, `styles.css`, and the relevant HTML entrypoint.
- New sections should usually be added as a new `*.html` file plus a small `render...` branch in `site.js`.
- Commissioned Work uses Cloudinary IDs `commissioned-work/1` through `commissioned-work/23`; upload it with `scripts/upload-commissioned-work-to-cloudinary.sh`.
- Keep the visual language minimal and consistent with the existing site unless explicitly asked to redesign it.
- Print sales use an external handoff to `shop.clairethomas.art`. Every gallery/lightbox link opens the photograph's canonical Fine Art product page directly.
- The live Horizon theme includes `shopify-theme-overrides/ct-product-consolidation.liquid` before `</body>`. It shows each photograph once in storefront and predictive-search grids, requires the contact email, and adds Fine Art, Framed, and Canvas choices to the product page while preserving separate Gelato fulfillment products underneath.
- The print catalog derives from the current non-commissioned gallery data. Each photograph is prepared for Fine Art Print, Framed Fine Art Print, and Canvas Print, with three aspect-ratio-matched sizes. Filename-based print IDs remain stable when galleries are added, deleted, or reordered. Run `node scripts/gelato-products.mjs` for a dry run, `--validate-templates` before creation, and `--audit` after removals.
- Create or resume the public catalog with `node scripts/gelato-products.mjs --execute --visible --concurrency 1`. The ignored state file prevents duplicate creation, and API throttling is retried with exponential backoff. Require `node scripts/gelato-products.mjs --strict-audit` to pass before declaring the 810-product catalog complete.
- The initial catalog is managed by LaunchAgent `art.clairethomas.gelato-catalog`; inspect it with `launchctl print gui/$(id -u)/art.clairethomas.gelato-catalog` and follow `.gelato-catalog-resume.log`. It stops restarting after the script completes successfully.
- After material workflow or structure changes, refresh this file so future sessions inherit the current repo shape.
